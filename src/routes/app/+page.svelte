<!-- This route always redirects server-side (see +page.server.js).
     This file exists only because SvelteKit requires a +page.svelte
     alongside +page.server.js; it is never actually rendered. -->
