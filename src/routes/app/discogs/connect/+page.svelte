<!-- This page only exists so SvelteKit registers the route as a valid action target.
     The load() function always redirects away to /app/settings, so this
     template is never actually rendered. -->
<div></div>
